# Integration test: Simple Math

Use configuration file: `private/config.toml`

# Steps
- Re-Initialize the db
- Use `kesoku chat` to test a simple math problem
- Use `kesoku chat -l` to list the session
- Use `kesoku chat --show-history` to show the chat history